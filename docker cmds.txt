docker run --name jobping-db \
  -e POSTGRES_PASSWORD=RoyalsStink816@ \
  -e POSTGRES_USER=jobping-admin \
  -e POSTGRES_DB=jobping \
  -v jobping-data:/var/lib/postgresql \
  -p 5432:5432 \
  -d postgres

docker exec -it jobping-db psql -U jpbping-admin -d postgres

docker stop jobping-db

docker start jobping-db

#start interactive CLI postgres - keywords are case sensitive (i.e. SELECT not select)
docker exec -it jobping-db psql -U jobping-admin -d jobping