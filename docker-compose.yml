services:
postgres:
  image: postgres:18.1
  restart: always
  environment:
    POSTGRES_USER: jobping-admin
    POSTGRES_PASSWORD: pecan5022
    POSTGRES_DB: jobping
  ports:
    - "5432:5432"
  volumes:
    - jobping_data:/var/lib/postgresql
volumes:
  jobping_data:
